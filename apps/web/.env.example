# apps/api (Cloudflare Worker) のベースURL。
# ローカル開発時は `bun run --filter api dev` (wrangler dev) の既定ポートを指す。
# apps/web は静的export（Cloudflare Pages配信）でサーバーサイド実行を持たず、
# ブラウザから直接apps/apiを呼び出すため、NEXT_PUBLIC_ prefixの変数として公開する。
NEXT_PUBLIC_API_BASE_URL=http://localhost:8787
